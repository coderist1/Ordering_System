# Local dev
VITE_API_BASE_URL=/api/v1

# Production (Railway) — also set in Railway frontend service if overriding build
# VITE_API_BASE_URL=https://ordering-system-backend-production.up.railway.app/api/v1
